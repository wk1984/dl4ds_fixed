# dl4ds_fixed

Try to fix issues when installing DL4DS

See detail in using DL4DS: https://github.com/carlos-gg/dl4ds

The codes are not changed. We add a 'setup.py' and 'Dockerfile' to make sure it could be installed by using Pip.
